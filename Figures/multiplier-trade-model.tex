\input{./._relpath-to-latexroot.ltx}
\documentclass[\latexroot/\projectname]{subfiles}
\input{\latexroot/@resources/subfile-setup.ltx}

\begin{document}

\begin{figure}[h]
  \begin{center}
    \includegraphics[alt={},width=\textwidth]{Figures/Images/multiplier-trade-model.jpg}
  \captionsetup{labelformat=empty}
  \caption{Consumption multipliers and trade linkages (model)}
  \end{center}
  \end{figure}
  
\noindent\parbox{\textwidth}{\footnotesize
  \textbf{Notes}: We vary parameter $\alpha$ which captures the degree of home bias for region 1 . The benchmark calibration is $\alpha=0.58$ (vertical line). We show the local and the aggregate consumption multiplier for a four-year horizon.
}

\vspace{1em}  % Add space after figure

% Smart bibliography: Only include bibliography if standalone AND has citations
\smartbib

\end{document}
